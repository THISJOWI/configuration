server:
  port: 8084
  address: 0.0.0.0

spring:
  application:
    name: password
  flyway:
    baseline-on-migrate: true

  datasource:
    url: jdbc:postgresql://cockroachdb:${DB_PORT}/password
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # can be: none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Temporarily disable Spring Cloud / Spring Boot compatibility verifier so the app can start
  cloud:
    compatibility-verifier:
      enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
  instance:
    prefer-hostname: true
    hostname: password
    lease-timeout-in-seconds: 10
    lease-renewal-interval-in-seconds: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true

# Spring Boot Admin Client configuration
spring.boot.admin.client:
  url: http://admin:8090
  instance:
    name: ${spring.application.name}
    service-url: http://password:8084
    management-url: http://password:8084/actuator
    health-url: http://password:8084/actuator/health

logging:
  level:
    uk.thisjowi.Password: DEBUG

kafka:
  topic:
    auth-events: auth-events

auth:
  service:
    url: http://api-gateway:8100/api/v1/auth

# JWT secret used to verify tokens locally if Authentication service is unavailable.
# Ideally both services should load the same secret from Vault or an environment
# variable. This default matches the Authentication service fallback `jwt.secret`.
app:
  jwt:
    secret: ${JWT_SECRET}

  kafka:
    bootstrap-servers: ${KAFKA_HOST}:${KAFKA_PORT}
    consumer:
      group-id: password-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  docker:
    compose:
      enabled: false

# OpenAPI Documentation configuration
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  info:
    title: Password Service API
    description: API for managing user passwords
    version: 1.0.0
  packages-to-scan: uk.thisjowi.Password
